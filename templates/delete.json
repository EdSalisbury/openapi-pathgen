{
  "summary": "Deletes ${lname}",
  "parameters": [
    {
      "in": "path",
      "name": "id",
      "schema": {
        "$ref": "#/components/schemas/uuid"
      },
      "required": true,
      "description": "UUID of the ${lname} to delete"
    }
  ],
  "tags": ["${sname}s"],
  "responses": {
    "200": {
      "description": "Deleted ${lname}.",
      "content": {
        "application/json": {
          "schema": {
            "$ref": "#/components/schemas/${sname}"
          }
        }
      }
    }
  }
}
