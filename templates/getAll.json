{
  "summary": "Gets all ${name}s",
  "tags": ["${name}s"],
  "responses": {
    "200": {
      "description": "All ${name}s.",
      "content": {
        "application/json": {
          "schema": {
            "$ref": "#/components/schemas/${name}s"
          }
        }
      }
    }
  }
}
