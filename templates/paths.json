{
	"paths": {
    	"/${sname}s": {
			"get": "${getAll}",
			"post": "${post}"
		},
		"/${sname}s/{id}": {
			"get": "${get}",
			"put": "${put}",
			"patch": "${patch}",
			"delete": "${delete}"
		}
	}
}